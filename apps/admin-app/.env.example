# Server-side only — NEVER use NEXT_PUBLIC_ prefix for this secret
SUPER_ADMIN_SECRET=your-super-admin-secret
ADMIN_WORKER_URL=https://admin-worker.trnpk.net

# Basic auth (used in middleware when Vercel Password Protection is unavailable)
BASIC_AUTH_USER=admin
BASIC_AUTH_PASSWORD=your-strong-password
